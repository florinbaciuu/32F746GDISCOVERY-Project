# Examples

Small applications in this directory should demonstrate one framework concept at a time:

- `media_player`: audio, storage and LVGL composition
- `image_viewer`: file decoding and display pipeline
- `dashboard`: reusable widgets and service data binding
- `terminal`: input, scrolling text and command dispatch

Examples must use public BSP and application service APIs only. They should not call HAL directly.

